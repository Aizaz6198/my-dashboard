import styles from './Navbar.module.css';

const Navbar = () => {
  return (
    <nav className={styles.navbar}>
      <div className={styles.navTitle}>Navbar</div>
    </nav>
  );
};

export default Navbar;
